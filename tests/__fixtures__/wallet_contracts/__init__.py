"""Hermetic Wallet REST contract fixtures — versioned and redacted.

Every fixture is a snapshot of a documented BudgetBakers Wallet OpenAPI
response. All sensitive values (API keys, real account numbers, addresses,
merchant names, notes) are replaced with placeholder tokens.

Fixture versioning: each directory under ``v<N>/`` represents a distinct
documented API version. Tests load the version they target.
"""
